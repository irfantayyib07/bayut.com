{
  "name": "bayut.com",
  "version": "1.0.0",
  "description": "",
  "main": "tailwind-default.config.js",
  "scripts": {
    "tailwind": "npx tailwindcss -i src/input.css -o build/css/style.css --watch",
    "buildcss": "npx postcss build/css/style.css -u cssnano -d build/css --no-map",
    "prettier": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prettier-plugin-tailwindcss": "^0.5.12"
  },
  "devDependencies": {
    "cssnano": "^6.1.2",
    "postcss-cli": "^11.0.0"
  }
}
